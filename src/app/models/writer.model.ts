export class Writer {
  constructor(
    public id: number,
    public name: string,
    public lastNames: string,
    public image: string,
    public country: string
  ) { }
}

/* export class Writer {
  id: number;
  name: string;
  lastNames: string;
  image: string;
  country: string;

  constructor(id: number, name: string, lastNames: string, image: string, country: string) {
    this.id = id;
    this.name = name;
    this.lastNames = lastNames;
    this.image = image;
    this.country = country;
  }
} */